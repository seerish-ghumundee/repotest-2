name: Trivy Scan

on:
  push:
    branches:
      - develop

jobs:
  trivy-scan:f
    uses: mnsltd/DEVOPS-githubworkflows/.github/workflows/trivy-java-ci.yml@develop
    with:
      java-version: '21'
      java-distribution: 'temurin'
      maven-command: 'mvn clean install'
      docker-build-command: 'docker build  --build-arg=CAR_VERSION=${VERSION} -t code:latest ./nicdwso2KubernetesExporter'
      maven-version: '3.6.3'
    secrets: inherit
